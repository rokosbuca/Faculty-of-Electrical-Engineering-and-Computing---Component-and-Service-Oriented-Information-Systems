/**
 * 
 * @author rsb
 */

// router
const router = require('express').Router();
const mapping = '/authentication';

// middlewares
const security = require('../security');

module.exports = {
    router
};