/**
 * 
 * @author rsb
 */

// router
const router = require('express').Router();
const mapping = '/statuses';

// middlewares
const security = require('../security');

module.exports = {
    router
};